# 矿山边坡工作台面合规性检查系统 — 需求规格说明书（SRS）

| 项目 | 内容 |
|------|------|
| **项目名称** | 基于 Cesium 的矿山边坡工作台面合规性检查系统 |
| **文档版本** | v1.0 |
| **编写日期** | 2026-07-24 |
| **文档目的** | 作为后续 AI 编码执行的权威需求基线 |
| **适用读者** | AI 编码代理、前端开发工程师、测绘工程师、矿山安全评审人员 |

---

## 目录

1. [引言](#1-引言)
2. [总体描述](#2-总体描述)
3. [法规与标准依据](#3-法规与标准依据)
4. [系统功能需求](#4-系统功能需求)
5. [核心算法需求](#5-核心算法需求)
6. [前端界面需求](#6-前端界面需求)
7. [数据需求](#7-数据需求)
8. [接口需求](#8-接口需求)
9. [非功能性需求](#9-非功能性需求)
10. [技术架构与依赖](#10-技术架构与依赖)
11. [项目目录结构](#11-项目目录结构)
12. [验收标准](#12-验收标准)
13. [附录](#13-附录)

---

## 1. 引言

### 1.1 项目背景

露天矿山开采过程中，边坡工作台面（安全平台、清扫平台、运输平台）的宽度直接关系到矿山安全生产。根据《金属非金属矿山安全规程》（GB 16423-2020），不同类型的平台均有明确的最小宽度要求。传统检查方式依赖人工现场测量，效率低、覆盖面有限、难以全面掌握整个矿区边坡状况。

随着实景三维建模技术（倾斜摄影测量、激光雷达扫描）的成熟，矿山可获取高精度三维点云/网格模型。本项目旨在基于 **CesiumJS** 三维前端引擎，加载矿山实景三维模型，自动分析识别边坡工作台面，测量其宽度，并将不合规区域（宽度低于阈值）高亮标注，实现数字化、可视化的合规性检查。

### 1.2 项目目标

| 序号 | 目标 | 衡量指标 |
|------|------|----------|
| G1 | 加载并流畅渲染矿山实景三维模型 | 模型加载时间 ≤ 10s，帧率 ≥ 30 FPS |
| G2 | 自动识别边坡工作台面 | 台面识别召回率 ≥ 85%，准确率 ≥ 80% |
| G3 | 自动测量并标注台面宽度 | 宽度测量误差 ≤ ±0.5m（模型精度范围内） |
| G4 | 不合规区域自动高亮预警 | 阈值可配置，高亮响应时间 ≤ 2s |
| G5 | 生成合规性检查报告 | 支持导出 PDF/Excel 报告 |

### 1.3 术语与定义

| 术语 | 定义 |
|------|------|
| **工作台面（Berm/Platform）** | 露天矿边坡上两个相邻台阶坡面之间的水平或近水平区域，包括安全平台、清扫平台和运输平台 |
| **安全平台** | 为保证边坡稳定和截留落石而设置的平台，宽度一般不小于 3m～5m |
| **清扫平台** | 用于清理边坡浮石的平台，人工清扫宽度不小于 6m，机械清扫不小于 8m |
| **运输平台** | 用于运输矿石或废石的平台，宽度根据运输设备确定，一般不小于 8m～12m |
| **台阶高度** | 相邻两个平台之间的垂直距离 |
| **坡面角** | 台阶坡面与水平面的夹角 |
| **3D Tiles** | Cesium 定义的大规模三维数据流式传输格式 |
| **实景三维模型** | 通过倾斜摄影测量或激光扫描生成的带纹理的三维网格模型 |
| **合规阈值** | 用户配置的最小允许台面宽度，默认 5m |

### 1.4 文档范围

本说明书涵盖系统的功能需求、核心算法逻辑、前端交互设计、数据格式定义、接口规范和非功能性要求。**不包含**后端服务器的部署运维细节和三维数据采集外业方案。

---

## 2. 总体描述

### 2.1 系统概述

系统是一个纯前端 Web 应用（可选轻量后端用于数据服务），基于 CesiumJS 三维地球引擎构建。用户加载矿山实景三维模型后，系统通过几何分析算法自动识别边坡工作台面，计算每个台面的宽度，并将结果以三维可视化的方式叠加展示在模型上，同时根据用户设定的合规阈值对不合规区域进行高亮预警。

### 2.2 用户角色

| 角色 | 描述 | 核心诉求 |
|------|------|----------|
| **矿山安全工程师** | 主要用户，负责边坡安全检查 | 快速定位不合规台面，生成检查报告 |
| **矿山管理人员** | 查看合规性汇总结果 | 了解整体合规率、风险分布 |
| **测绘工程师** | 负责三维数据采集与更新 | 上传/替换三维模型数据 |
| **系统管理员** | 管理用户和系统配置 | 配置阈值参数、用户权限 |

### 2.3 运行环境

| 项目 | 要求 |
|------|------|
| **客户端浏览器** | Chrome ≥ 90 / Edge ≥ 90 / Firefox ≥ 88（需 WebGL 2.0 支持） |
| **GPU** | 独立显卡，显存 ≥ 2GB，支持 WebGL 2.0 |
| **内存** | ≥ 8GB（推荐 16GB） |
| **网络** | 带宽 ≥ 10Mbps（模型数据加载） |
| **服务端（可选）** | 任何能提供静态文件服务的 HTTP 服务器（Nginx / Node.js） |

### 2.4 约束条件

1. 系统以前端为主，核心分析逻辑在浏览器端完成，不依赖重型后端计算服务
2. 三维模型数据需预先转换为 3D Tiles 格式
3. 模型需带有准确的地理空间坐标参考系统（CGCS2000 或 WGS84）
4. 分析精度受原始模型精度限制（摄影测量模型精度通常为厘米至分米级）

---

## 3. 法规与标准依据

### 3.1 主要法规标准

| 标准编号 | 标准名称 | 相关内容 |
|----------|----------|----------|
| **GB 16423-2020** | 金属非金属矿山安全规程 | 露天矿边坡平台宽度、台阶高度、坡面角要求 |
| **GB 50070** | 矿山电力设计规范 | — |
| **AQ 2005** | 金属非金属矿山安全标准化规范 | 安全管理体系 |
| **GB/T 18341** | 大比例尺地形图机助制图技术规范 | 地形数据精度要求 |

### 3.2 GB 16423-2020 关键条款摘录

> ⚠️ 以下为公开条款摘要，实际部署应以标准正式文本为准。

| 平台类型 | 最小宽度要求 | 条款参考 |
|----------|-------------|----------|
| 安全平台 | ≥ 3m（一般要求），最终边坡 ≥ 5m | 5.2.5.x |
| 人工清扫平台 | ≥ 6m | 5.2.5.x |
| 机械清扫平台 | ≥ 8m（且满足设备要求） | 5.2.5.x |
| 运输平台 | 根据设备确定，一般 ≥ 8m～12m | 5.2.5.x |

| 参数 | 要求 |
|------|------|
| 台阶高度 | 根据矿岩性质确定，一般 ≤ 15m（坚硬矿岩可适当增大） |
| 坡面角 | 根据矿岩性质和台阶高度确定，一般 45°～75° |
| 最终边坡角 | ≤ 55°（根据岩体稳定性分析确定） |

### 3.3 系统合规阈值默认值

系统采用可配置阈值设计，默认值参照下表，用户可在界面中修改：

```yaml
default_thresholds:
  safety_platform_min_width: 5.0    # 安全平台最小宽度（米）
  cleaning_platform_min_width: 6.0  # 清扫平台最小宽度（米）
  transport_platform_min_width: 8.0 # 运输平台最小宽度（米）
  max_bench_height: 15.0            # 最大台阶高度（米）
  max_slope_angle: 75.0             # 最大坡面角（度）
  max_final_slope_angle: 55.0       # 最大最终边坡角（度）
```

---

## 4. 系统功能需求

### 4.1 功能模块总览

```
矿山边坡工作台面合规性检查系统
├── F1 三维模型加载与管理
│   ├── F1.1 加载 3D Tiles 实景模型
│   ├── F1.2 模型定位与视角控制
│   ├── F1.3 模型可见性/透明度控制
│   └── F1.4 多模型切换管理
├── F2 边坡台面自动识别
│   ├── F2.1 模型几何数据提取
│   ├── F2.2 台面区域识别
│   ├── F2.3 台面类型分类
│   └── F2.4 人工修正接口
├── F3 台面宽度测量
│   ├── F3.1 自动宽度计算
│   ├── F3.2 宽度标注显示
│   └── F3.3 手动测量工具
├── F4 合规性检查与预警
│   ├── F4.1 阈值配置
│   ├── F4.2 不合规区域高亮
│   ├── F4.3 风险等级评定
│   └── F4.4 统计概览
├── F5 报告生成
│   ├── F5.1 合规检查报告
│   ├── F5.2 导出 PDF
│   └── F5.3 导出 Excel
├── F6 场景工具
│   ├── F6.1 剖切分析
│   ├── F6.2 量测工具
│   ├── F6.3 视角书签
│   └── F6.4 截图功能
└── F7 系统设置
    ├── F7.1 用户偏好
    └── F7.2 图层管理
```

### 4.2 F1 — 三维模型加载与管理

#### F1.1 加载 3D Tiles 实景模型

| 属性 | 要求 |
|------|------|
| **输入** | 3D Tiles 数据集 URL 或本地 tileset.json 文件路径 |
| **处理** | 使用 `Cesium3DTileset` 加载数据集，配置 `maximumScreenSpaceError` 控制 LOD |
| **输出** | 模型在三维场景中渲染显示 |
| **优先级** | P0（必须） |

**详细要求：**

- 支持通过 URL 加载远程 3D Tiles 数据集
- 支持用户上传本地 tileset.json（及其关联的数据文件）
- 模型加载时显示进度条
- 加载完成后自动飞至模型范围（`viewer.flyTo(tileset)`）
- 支持配置以下加载参数：
  ```javascript
  {
    maximumScreenSpaceError: 16,      // LOD 精度，值越小精度越高
    maximumMemoryUsage: 1024,         // 最大内存使用（MB）
    dynamicScreenSpaceError: true,    // 动态屏幕空间误差
    skipLevelOfDetail: true,          // 跳级加载，加速首屏
    baseScreenSpaceError: 1024,
    skipScreenSpaceErrorFactor: 16,
    skipLevels: 1,
    immediatelyLoadDesiredLevelOfDetail: false,
    loadSiblings: false,
    cullWithChildrenBounds: true
  }
  ```

#### F1.2 模型定位与视角控制

| 属性 | 要求 |
|------|------|
| **功能** | 提供模型快速定位和多种预设视角 |
| **优先级** | P0 |

**详细要求：**

- 提供「定位至模型」按钮，一键飞至模型范围
- 提供俯视、侧视、正视三个预设视角快捷按钮
- 支持鼠标左键旋转、右键平移、滚轮缩放（Cesium 默认操作）
- 显示当前相机位置（经度、纬度、高度）和视线方向角度

#### F1.3 模型可见性/透明度控制

| 属性 | 要求 |
|------|------|
| **功能** | 控制模型显示属性，便于观察台面 |
| **优先级** | P1 |

**详细要求：**

- 模型整体可见性开关（显示/隐藏）
- 模型透明度滑块（0% ~ 100%）
- 点云模式：点大小调节（1~10px）
- 网格模式：填充/线框切换
- 可按高度区间过滤显示（例如只显示某高程段的模型）

#### F1.4 多模型切换管理

| 属性 | 要求 |
|------|------|
| **功能** | 支持加载多个时间段的模型（用于对比分析） |
| **优先级** | P2 |

### 4.3 F2 — 边坡台面自动识别

#### F2.1 模型几何数据提取

| 属性 | 要求 |
|------|------|
| **输入** | 已加载的 3D Tiles 模型 |
| **处理** | 从模型中提取几何表面数据（顶点、三角面片） |
| **输出** | 结构化几何数据（点云或网格），用于后续分析 |
| **优先级** | P0 |

**技术方案（两种路径，需均实现并可选切换）：**

**路径 A — 基于 3D Tiles 瓦片内容提取：**

- 遍历 `Cesium3DTileset` 的瓦片树
- 从每个瓦片的 `Cesium3DTileContent` 中提取 `model.components` 的顶点和索引数据
- 将所有几何数据统一至世界坐标系
- 输出为 `Float32Array` 格式的顶点数组 + `Uint16Array/Uint32Array` 的索引数组

**路径 B — 基于地形高程采样（适用于可获取 DEM 的情况）：**

- 在模型覆盖范围内生成均匀网格采样点
- 使用 `scene.sampleHeight()` 或 `scene.clampToHeight()` 获取每个采样点的地表高程
- 构建规则格网 DEM（数字高程模型）
- 输出为 `{x, y, z}` 点云格式

**采样参数：**

```javascript
{
  samplingResolution: 0.5,  // 采样间距（米），决定分析精度
  samplingBounds: {         // 采样范围（自动从模型 bounding sphere 计算）
    minLon, minLat, maxLon, maxLat
  },
  maxSamplingPoints: 500000 // 最大采样点数，防止内存溢出
}
```

#### F2.2 台面区域识别

| 属性 | 要求 |
|------|------|
| **输入** | 几何数据（点云或 DEM） |
| **处理** | 识别近水平面区域作为台面候选 |
| **输出** | 台面区域多边形列表（每个台面含边界多边形、中心线、高程值） |
| **优先级** | P0 |

**识别算法逻辑（详见第 5 章）：**

1. **坡度计算**：对每个采样点，计算其局部坡度角
2. **坡度阈值分割**：坡度 < `slope_threshold`（默认 15°）的点标记为「近水平面」候选
3. **连通区域分析**：将相邻的近水平面点聚类为连通区域
4. **面积过滤**：面积 < `min_area`（默认 10m²）的区域过滤掉（噪点）
5. **形状验证**：验证区域是否呈带状特征（长宽比 > 一定阈值），排除大面积平坦底部
6. **高程分组**：按高程将台面分组，同一高程层为一个台面
7. **边界提取**：对每个台面区域提取外轮廓多边形（使用凸包或 Alpha Shape 算法）

**输出数据结构：**

```typescript
interface Platform {
  id: string;                    // 台面唯一标识
  type: 'safety' | 'cleaning' | 'transport' | 'unknown'; // 台面类型
  elevation: number;             // 台面高程（米）
  area: number;                  // 台面面积（平方米）
  boundaryPolygon: Cartesian3[]; // 边界多边形顶点（世界坐标）
  centerLine: {                  // 台面中心线（用于宽度测量）
    start: Cartesian3;
    end: Cartesian3;
    points: Cartesian3[];
  };
  widthMeasurements: WidthMeasurement[]; // 宽度测量点列表
  averageWidth: number;          // 平均宽度
  minWidth: number;              // 最小宽度
  maxWidth: number;              // 最大宽度
  isCompliant: boolean;          // 是否合规
  complianceIssues: string[];    // 不合规问题描述
}
```

#### F2.3 台面类型分类

| 属性 | 要求 |
|------|------|
| **输入** | 识别出的台面 |
| **处理** | 根据台面宽度、高程间隔、位置特征分类 |
| **输出** | 台面类型标签 |
| **优先级** | P1 |

**分类规则：**

| 类型 | 判断条件 |
|------|----------|
| 安全平台 | 宽度 < 6m，位于边坡非底部位置 |
| 清扫平台 | 宽度 6m～8m，位于边坡中部 |
| 运输平台 | 宽度 > 8m，有明显道路痕迹或与出入口连通 |
| 未知 | 不满足上述条件，需人工确认 |

> 注：自动分类为辅助参考，用户可手动修正。

#### F2.4 人工修正接口

| 属性 | 要求 |
|------|------|
| **功能** | 允许用户对自动识别结果进行增删改 |
| **优先级** | P1 |

**支持操作：**

- 删除误识别的台面
- 手动绘制新台面边界（多边形绘制工具）
- 修改台面类型
- 合并/拆分台面区域

### 4.4 F3 — 台面宽度测量

#### F3.1 自动宽度计算

| 属性 | 要求 |
|------|------|
| **输入** | 台面区域边界多边形 |
| **处理** | 沿台面走向计算垂直方向的宽度 |
| **输出** | 宽度测量点列表（每点含位置、宽度值、法线方向） |
| **优先级** | P0 |

**测量算法逻辑：**

1. 提取台面中心线（骨架线）：对台面边界多边形进行骨架化提取
   - 可使用「迭代距离变换」或「Delaunay 三角剖分中轴」算法
2. 沿中心线等间距采样（默认间距 2m）
3. 在每个采样点，计算中心线的法线方向（垂直于走向）
4. 沿法线方向向两侧延伸射线，与台面边界求交
5. 两交点之间的距离即为该位置的台面宽度
6. 记录所有采样点的宽度值

**输出数据结构：**

```typescript
interface WidthMeasurement {
  position: Cartesian3;     // 测量点位置（中心线上）
  width: number;            // 宽度值（米）
  leftEdge: Cartesian3;     // 左侧边界点
  rightEdge: Cartesian3;    // 右侧边界点
  normal: Cartesian3;       // 法线方向
  isCompliant: boolean;     // 该点宽度是否合规
}
```

#### F3.2 宽度标注显示

| 属性 | 要求 |
|------|------|
| **功能** | 在三维场景中可视化展示宽度测量结果 |
| **优先级** | P0 |

**显示要求：**

- 在每个测量点位置绘制宽度标注线（连接左右边界点）
- 标注线上显示宽度数值（单位：米，保留 1 位小数）
- 合规点标注线颜色为绿色（`#00FF00`），不合规为红色（`#FF0000`）
- 标注文字始终面向相机（`Cesium.LabelGraphics` 的 `eyeOffset` 和 `pixelOffsetScale`）
- 支持标注密度调节：稀疏（每 10m 一个）、中等（每 5m 一个）、密集（每 2m 一个）
- 鼠标悬停标注线时显示详细信息气泡（位置坐标、宽度值、坡度等）

#### F3.3 手动测量工具

| 属性 | 要求 |
|------|------|
| **功能** | 提供手动两点测量工具，用于验证自动结果 |
| **优先级** | P1 |

**支持：**

- 两点直线距离测量（贴模型表面）
- 水平距离 / 空间距离 / 垂直高差三种模式
- 测量结果实时显示

### 4.5 F4 — 合规性检查与预警

#### F4.1 阈值配置

| 属性 | 要求 |
|------|------|
| **功能** | 提供界面配置合规阈值 |
| **优先级** | P0 |

**配置项：**

| 配置项 | 类型 | 默认值 | 范围 |
|--------|------|--------|------|
| 安全平台最小宽度 | 数值 | 5.0m | 1～20m |
| 清扫平台最小宽度 | 数值 | 6.0m | 1～20m |
| 运输平台最小宽度 | 数值 | 8.0m | 1～30m |
| 通用最小宽度（简化模式） | 数值 | 5.0m | 1～30m |
| 坡度阈值（台面识别） | 数值 | 15° | 5°～30° |
| 最小台面面积 | 数值 | 10m² | 1～100m² |
| 宽度测量间距 | 数值 | 2m | 0.5～10m |

- 配置变更后自动重新检查，刷新高亮显示
- 支持保存配置到 `localStorage`
- 支持恢复默认值

#### F4.2 不合规区域高亮

| 属性 | 要求 |
|------|------|
| **功能** | 对宽度低于阈值的区域进行视觉高亮预警 |
| **优先级** | P0 |

**高亮效果：**

- 不合规的宽度测量段用**红色半透明面**填充覆盖（`Cesium.PolygonGraphics` 或 `Cesium.PolylineGraphics`）
- 不合规区域边界用**红色虚线**描边
- 添加**脉冲动画效果**（透明度周期变化），吸引注意
- 不合规台面的标签文字变红并加粗
- 视图右下角弹出**预警通知卡片**，显示不合规台面数量

**高亮渲染参数：**

```javascript
{
  fillColor: Cesium.Color.RED.withAlpha(0.4),       // 填充色
  outlineColor: Cesium.Color.RED,                     // 描边色
  outlineWidth: 2,                                    // 描边宽度
  pulseAnimation: true,                               // 脉冲动画
  pulsePeriod: 2000,                                  // 脉冲周期（ms）
  pulseMinAlpha: 0.2,                                 // 最小透明度
  pulseMaxAlpha: 0.6                                  // 最大透明度
}
```

#### F4.3 风险等级评定

| 属性 | 要求 |
|------|------|
| **功能** | 对每个不合规台面评定风险等级 |
| **优先级** | P1 |

**风险等级划分：**

| 等级 | 颜色 | 判定条件 | 说明 |
|------|------|----------|------|
| 高风险 | 红色 | 宽度 < 阈值的 50% | 严重不足，立即停工整改 |
| 中风险 | 橙色 | 阈值的 50% ≤ 宽度 < 阈值的 80% | 明显不足，限期整改 |
| 低风险 | 黄色 | 阈值的 80% ≤ 宽度 < 阈值 | 略微不足，观察监测 |
| 合规 | 绿色 | 宽度 ≥ 阈值 | 满足要求 |

#### F4.4 统计概览

| 属性 | 要求 |
|------|------|
| **功能** | 提供合规性检查结果统计面板 |
| **优先级** | P0 |

**统计内容：**

- 台面总数 / 合规数 / 不合规数
- 合规率（百分比，带进度条）
- 按风险等级分布（饼图）
- 按台面类型分布（柱状图）
- 不合规台面列表（可点击定位至三维场景）
- 整体评估结论文字（合格/不合格）

### 4.6 F5 — 报告生成

#### F5.1 合规检查报告

| 属性 | 要求 |
|------|------|
| **功能** | 生成结构化的合规性检查报告 |
| **优先级** | P1 |

**报告内容：**

1. **封面信息**：矿山名称、检查日期、检查人员、报告编号
2. **检查依据**：引用的法规标准
3. **检查范围**：模型覆盖区域、涉及边坡
4. **统计汇总**：台面总数、合规率、风险分布
5. **详细清单**：每个台面的编号、类型、位置、宽度值、合规判定
6. **不合规问题列表**：问题描述、风险等级、整改建议
7. **三维截图**：关键不合规区域的三维场景截图
8. **结论与建议**：整体评价、整改优先级建议

#### F5.2 导出 PDF

- 使用 `jsPDF` 或 `html2canvas + jsPDF` 生成
- 包含文字内容和三维截图
- A4 纵向排版

#### F5.3 导出 Excel

- 使用 `SheetJS (xlsx)` 生成
- 包含台面清单表和统计汇总表

### 4.7 F6 — 场景工具

#### F6.1 剖切分析

| 属性 | 要求 |
|------|------|
| **功能** | 对三维模型进行剖切，观察边坡断面 |
| **优先级** | P1 |

**支持：**

- 使用 `Cesium.ClippingPlaneCollection` 实现模型剖切
- 支持平面剖切、多边形剖切
- 剖切面上可叠加显示断面轮廓线和台面宽度标注
- 剖切线可在地图上拖动调整位置

#### F6.2 量测工具

| 属性 | 要求 |
|------|------|
| **功能** | 提供多种量测工具 |
| **优先级** | P1 |

- 距离测量（两点间空间距离）
- 面积测量（多边形面积）
- 高差测量（两点间垂直高差）
- 角度测量（三点间夹角）

#### F6.3 视角书签

| 属性 | 要求 |
|------|------|
| **功能** | 保存和恢复常用视角 |
| **优先级** | P2 |

#### F6.4 截图功能

| 属性 | 要求 |
|------|------|
| **功能** | 截取当前三维场景画面 |
| **优先级** | P1 |

- 使用 `viewer.canvas.toDataURL()` 实现
- 支持全屏截图和区域截图
- 截图可标注注记后保存

### 4.8 F7 — 系统设置

#### F7.1 用户偏好

- 主题切换（亮色/暗色）
- 语言切换（中文/英文）— 中文为默认
- 标注密度偏好
- 分析精度偏好

#### F7.2 图层管理

- 管理多个图层：模型层、台面层、标注层、高亮层
- 每个图层可独立控制显隐

---

## 5. 核心算法需求

本章详细描述边坡台面识别和宽度计算的核心算法逻辑。AI 编码时需严格按照此算法方案实现。

### 5.1 算法整体流程

```
输入：3D Tiles 实景模型
  │
  ▼
[Step 1] 几何数据提取
  │  从 3D Tiles 中提取顶点/三角面数据
  │  或通过 sampleHeight 生成规则格网 DEM
  ▼
[Step 2] 坡度计算
  │  对每个采样点计算局部坡度角
  ▼
[Step 3] 台面候选区域提取
  │  坡度 < 阈值 → 近水平面候选
  │  连通区域聚类 → 候选台面
  │  面积/形状过滤 → 有效台面
  ▼
[Step 4] 台面边界与骨架提取
  │  边界多边形提取（Alpha Shape / 凸包）
  │  骨架线（中心线）提取
  ▼
[Step 5] 宽度测量
  │  沿骨架线等间距采样
  │  法线方向射线求交 → 宽度
  ▼
[Step 6] 合规性判定
  │  宽度 < 阈值 → 不合规 → 高亮
  │  宽度 ≥ 阈值 → 合规
  ▼
输出：台面列表 + 宽度标注 + 合规判定
```

### 5.2 Step 1 — 几何数据提取

#### 方案 A：从 3D Tiles 提取（推荐，精度高）

```javascript
/**
 * 从 3D Tiles 模型中提取几何数据
 * @param {Cesium3DTileset} tileset
 * @returns {Promise<{positions: Float32Array, indices: Uint32Array}>}
 */
async function extractGeometryFromTileset(tileset) {
  const positions = [];
  const indices = [];

  // 遍历所有已加载的瓦片
  for (const tile of tileset.tiles) {
    const content = tile.content;
    const model = content.model; // Cesium3DTileModel 或类似

    // 遍历 model 中的 primitive
    for (const node of model.scene.nodes) {
      // 提取顶点和索引
      // 注意：需根据 Cesium 版本的内部 API 适配
    }
  }

  return { positions: new Float32Array(positions), indices: new Uint32Array(indices) };
}
```

> **注意**：Cesium 的内部瓦片数据结构在不同版本间可能变化。如果直接提取困难，使用方案 B。

#### 方案 B：高程采样生成 DEM（备选，更稳定）

```javascript
/**
 * 通过高程采样生成规则格网 DEM
 * @param {Viewer} viewer
 * @param {Object} bounds - {minLon, minLat, maxLon, maxLat}
 * @param {number} resolution - 采样间距（米）
 * @returns {Promise<{grid: Float32Array, cols: number, rows: number, bounds: Object}>}
 */
async function generateDEM(viewer, bounds, resolution) {
  // 1. 将米间距转换为经纬度间距
  const earthRadius = 6378137;
  const latCenter = (bounds.minLat + bounds.maxLat) / 2;
  const lonStep = (resolution / earthRadius) * (180 / Math.PI) / Math.cos(latCenter * Math.PI / 180);
  const latStep = (resolution / earthRadius) * (180 / Math.PI);

  const cols = Math.ceil((bounds.maxLon - bounds.minLon) / lonStep);
  const rows = Math.ceil((bounds.maxLat - bounds.minLat) / latStep);

  // 2. 生成采样点
  const positions = [];
  for (let row = 0; row < rows; row++) {
    for (let col = 0; col < cols; col++) {
      const lon = bounds.minLon + col * lonStep;
      const lat = bounds.minLat + row * latStep;
      positions.push(Cesium.Cartographic.fromDegrees(lon, lat));
    }
  }

  // 3. 分批采样高程（避免一次性采样过多）
  const batchSize = 10000;
  const grid = new Float32Array(cols * rows);

  for (let i = 0; i < positions.length; i += batchSize) {
    const batch = positions.slice(i, i + batchSize);
    // 使用 scene.sampleHeight 采样（需要模型已加载）
    const cartesians = batch.map(p => {
      const height = viewer.scene.sampleHeight(p);
      return { ...p, height };
    });

    for (let j = 0; j < cartesians.length; j++) {
      grid[i + j] = cartesians[j].height;
    }
  }

  return { grid, cols, rows, bounds, lonStep, latStep };
}
```

### 5.3 Step 2 — 坡度计算

对 DEM 网格中每个点，使用有限差分法计算坡度：

```javascript
/**
 * 计算每个网格点的坡度角
 * @param {Float32Array} grid - DEM 高程网格
 * @param {number} cols
 * @param {number} rows
 * @param {number} cellSize - 网格间距（米）
 * @returns {Float32Array} - 坡度角（度）
 */
function calculateSlope(grid, cols, rows, cellSize) {
  const slope = new Float32Array(cols * rows);

  for (let row = 1; row < rows - 1; row++) {
    for (let col = 1; col < cols - 1; col++) {
      const idx = row * cols + col;

      // Horn 法计算偏导数
      const dz_dx = (
        (grid[row-1]*1 + grid[idx-cols]*1 + grid[row+1]*1) * -1 +
        (grid[row-1]*1 + grid[row+1]*1) * 0 +
        (grid[row-1+cols]*1 + grid[idx+cols]*1 + grid[row+1+cols]*1) * 1
      ) / (8 * cellSize);

      const dz_dy = (
        (grid[row-1] + grid[row-1+1] + grid[row-1-1]) * -1 +
        (grid[idx-cols] + grid[idx+cols]) * 0 +
        (grid[row+1-1] + grid[row+1] + grid[row+1+1]) * 1
      ) / (8 * cellSize);

      // 简化的 Horn 公式（注意：上面的索引需要仔细对齐）
      // 标准实现：
      const a = grid[(row-1)*cols + (col-1)];
      const b = grid[(row-1)*cols + col];
      const c = grid[(row-1)*cols + (col+1)];
      const d = grid[row*cols + (col-1)];
      const f = grid[row*cols + (col+1)];
      const g = grid[(row+1)*cols + (col-1)];
      const h = grid[(row+1)*cols + col];
      const i_val = grid[(row+1)*cols + (col+1)];

      const dzdx = ((c + 2*f + i_val) - (a + 2*d + g)) / (8 * cellSize);
      const dzdy = ((g + 2*h + i_val) - (a + 2*b + c)) / (8 * cellSize);

      const slopeAngle = Math.atan(Math.sqrt(dzdx*dzdx + dzdy*dzdy)) * (180 / Math.PI);
      slope[idx] = slopeAngle;
    }
  }

  // 边界点坡度设为 0（或复制相邻值）
  // ... 边界处理

  return slope;
}
```

### 5.4 Step 3 — 台面候选区域提取

```javascript
/**
 * 从坡度数据中提取台面候选区域
 * @param {Float32Array} slope - 坡度网格
 * @param {number} cols
 * @param {number} rows
 * @param {Object} params - {slopeThreshold, minArea, cellSize}
 * @returns {Platform[]}
 */
function extractPlatforms(slope, cols, rows, params) {
  const { slopeThreshold = 15, minArea = 10, cellSize = 0.5 } = params;

  // 1. 二值化：坡度 < 阈值的点为候选
  const binary = new Uint8Array(cols * rows);
  for (let i = 0; i < slope.length; i++) {
    binary[i] = slope[i] < slopeThreshold ? 1 : 0;
  }

  // 2. 连通区域标记（4 连通或 8 连通，使用 BFS 或 Union-Find）
  const labels = new Int32Array(cols * rows);
  let labelCount = 0;
  const labelAreas = new Map();

  for (let i = 0; i < binary.length; i++) {
    if (binary[i] === 1 && labels[i] === 0) {
      labelCount++;
      const area = bfsLabel(binary, labels, i, cols, rows, labelCount);
      labelAreas.set(labelCount, area);
    }
  }

  // 3. 面积过滤
  const minCells = minArea / (cellSize * cellSize);
  const validLabels = new Set();
  for (const [label, area] of labelAreas) {
    if (area >= minCells) {
      validLabels.add(label);
    }
  }

  // 4. 对每个有效连通区域提取边界和特征
  const platforms = [];
  for (const label of validLabels) {
    const cells = getCellsByLabel(labels, label, cols, rows);
    const platform = analyzePlatformRegion(cells, cols, rows, cellSize);
    platforms.push(platform);
  }

  return platforms;
}
```

### 5.5 Step 4 — 台面边界与骨架提取

#### 边界提取 — Alpha Shape 算法

```javascript
/**
 * 使用 Alpha Shape 算法提取点集边界
 * Alpha Shape 是凸包的推广，能提取凹边界
 * @param {Cartesian3[]} points
 * @param {number} alpha - Alpha 参数（决定边界贴合程度）
 * @returns {Cartesian3[]} - 边界多边形顶点（有序）
 */
function alphaShapeBoundary(points, alpha) {
  // 1. 构建 Delaunay 三角剖分
  const triangulation = delaunayTriangulate(points);

  // 2. 过滤三角形：外接圆半径 > 1/alpha 的三角形被移除
  const filteredTriangles = triangulation.filter(tri => {
    const circumradius = calculateCircumradius(tri, points);
    return circumradius <= 1 / alpha;
  });

  // 3. 提取边界边：只属于一个三角形的边
  const edgeCount = new Map();
  for (const tri of filteredTriangles) {
    const edges = [
      [tri[0], tri[1]],
      [tri[1], tri[2]],
      [tri[0], tri[2]]
    ];
    for (const edge of edges) {
      const key = edge.sort().join(',');
      edgeCount.set(key, (edgeCount.get(key) || 0) + 1);
    }
  }

  const boundaryEdges = [];
  for (const [key, count] of edgeCount) {
    if (count === 1) {
      boundaryEdges.push(key.split(',').map(Number));
    }
  }

  // 4. 连接边界边为有序多边形
  return orderEdgesToPolygon(boundaryEdges);
}
```

#### 骨架线提取

```javascript
/**
 * 提取台面区域的骨架线（中心线）
 * 使用基于距离变换的细化算法
 * @param {Uint8Array} binaryMask - 台面区域的二值掩膜
 * @param {number} cols
 * @param {number} rows
 * @returns {Cartesian3[]} - 骨架线上的点（有序）
 */
function extractSkeleton(binaryMask, cols, rows) {
  // 1. 计算距离变换：每个点到最近边界点的距离
  const distanceTransform = computeDistanceTransform(binaryMask, cols, rows);

  // 2. 提取局部极大值点作为骨架节点
  const skeletonPoints = findLocalMaxima(distanceTransform, cols, rows);

  // 3. 按连通性排序骨架点，形成有序中心线
  const orderedSkeleton = orderSkeletonPoints(skeletonPoints, distanceTransform, cols, rows);

  // 4. 道格拉斯-普克算法简化
  const simplified = douglasPeuckerSimplify(orderedSkeleton, 0.5);

  return simplified;
}
```

### 5.6 Step 5 — 宽度测量

```javascript
/**
 * 沿骨架线测量台面宽度
 * @param {Cartesian3[]} skeleton - 骨架线点（有序）
 * @param {Cartesian3[]} boundary - 台面边界多边形
 * @param {number} sampleInterval - 采样间距（米）
 * @returns {WidthMeasurement[]}
 */
function measureWidths(skeleton, boundary, sampleInterval = 2.0) {
  const measurements = [];

  // 1. 沿骨架线等间距重采样
  const resampled = resamplePolyline(skeleton, sampleInterval);

  for (let i = 0; i < resampled.length; i++) {
    const point = resampled[i];

    // 2. 计算该点的切线方向（用前后点差分）
    const prev = resampled[Math.max(0, i - 1)];
    const next = resampled[Math.min(resampled.length - 1, i + 1)];
    const tangent = Cesium.Cartesian3.subtract(next, prev, new Cesium.Cartesian3());
    Cesium.Cartesian3.normalize(tangent, tangent);

    // 3. 计算法线方向（切线旋转 90°，在水平面上）
    const up = Cesium.Cartesian3.UNIT_Z;
    const normal = Cesium.Cartesian3.cross(tangent, up, new Cesium.Cartesian3());
    Cesium.Cartesian3.normalize(normal, normal);

    // 4. 沿法线方向向两侧发射射线，与边界多边形求交
    const leftEdge = rayPolygonIntersection(point, Cesium.Cartesian3.negate(normal, new Cesium.Cartesian3()), boundary);
    const rightEdge = rayPolygonIntersection(point, normal, boundary);

    if (leftEdge && rightEdge) {
      // 5. 计算宽度（水平距离）
      const width = Cesium.Cartesian3.distance(
        new Cesium.Cartesian3(leftEdge.x, leftEdge.y, 0),
        new Cesium.Cartesian3(rightEdge.x, rightEdge.y, 0)
      );

      measurements.push({
        position: point,
        width: width,
        leftEdge: leftEdge,
        rightEdge: rightEdge,
        normal: normal,
        isCompliant: null // 在合规检查步骤中判定
      });
    }
  }

  return measurements;
}
```

### 5.7 Step 6 — 合规性判定

```javascript
/**
 * 对每个台面进行合规性判定
 * @param {Platform[]} platforms
 * @param {Object} thresholds - 宽度阈值配置
 * @returns {Platform[]} - 含合规判定结果的台面列表
 */
function checkCompliance(platforms, thresholds) {
  for (const platform of platforms) {
    // 根据台面类型选择对应阈值
    let threshold;
    switch (platform.type) {
      case 'safety': threshold = thresholds.safety_platform_min_width; break;
      case 'cleaning': threshold = thresholds.cleaning_platform_min_width; break;
      case 'transport': threshold = thresholds.transport_platform_min_width; break;
      default: threshold = thresholds.general_min_width || 5.0;
    }

    // 逐测量点判定
    let compliantCount = 0;
    for (const measurement of platform.widthMeasurements) {
      measurement.isCompliant = measurement.width >= threshold;

      // 风险等级评定
      if (measurement.width < threshold * 0.5) {
        measurement.riskLevel = 'high';
      } else if (measurement.width < threshold * 0.8) {
        measurement.riskLevel = 'medium';
      } else if (measurement.width < threshold) {
        measurement.riskLevel = 'low';
      } else {
        measurement.riskLevel = 'none';
      }

      if (measurement.isCompliant) compliantCount++;
    }

    // 台面整体判定：存在任一不合规点则标记为不合规
    platform.isCompliant = compliantCount === platform.widthMeasurements.length;
    platform.complianceRate = compliantCount / platform.widthMeasurements.length;

    // 统计宽度信息
    const widths = platform.widthMeasurements.map(m => m.width);
    platform.minWidth = Math.min(...widths);
    platform.maxWidth = Math.max(...widths);
    platform.averageWidth = widths.reduce((a, b) => a + b, 0) / widths.length;

    // 生成问题描述
    if (!platform.isCompliant) {
      platform.complianceIssues = generateIssueDescription(platform, threshold);
    }
  }

  return platforms;
}
```

### 5.8 性能优化策略

| 策略 | 说明 |
|------|------|
| **Web Worker 并行计算** | 将 DEM 生成、坡度计算、连通区域分析等耗时任务放入 Web Worker，避免阻塞 UI 线程 |
| **分块处理** | 大区域模型分块分析，逐块完成并流式显示结果 |
| **LOD 策略** | 根据相机距离动态调整分析精度（远距离低精度，近距离高精度） |
| **结果缓存** | 分析结果缓存到内存，参数未变更时不重复计算 |
| **渐进式渲染** | 台面识别完成后逐个渲染标注，而非一次性全部渲染（避免卡顿） |
| **采样优化** | 大面积区域使用自适应采样（平坦区域稀疏，复杂区域密集） |

---

## 6. 前端界面需求

### 6.1 整体布局

系统采用经典的三栏布局：

```
┌─────────────────────────────────────────────────────────┐
│                      顶部工具栏                           │
├──────────┬─────────────────────────────┬────────────────┤
│          │                             │                │
│  左侧    │                             │    右侧面板     │
│  图层    │      三维场景视口             │  ┌──────────┐  │
│  面板    │      (Cesium Viewer)        │  │ 统计概览  │  │
│          │                             │  ├──────────┤  │
│          │                             │  │ 台面列表  │  │
│          │                             │  ├──────────┤  │
│          │                             │  │ 阈值配置  │  │
│          │                             │  └──────────┘  │
│          │                             │                │
├──────────┴─────────────────────────────┴────────────────┤
│                      底部状态栏                           │
└─────────────────────────────────────────────────────────┘
```

### 6.2 各区域详细需求

#### 6.2.1 顶部工具栏

- **左侧**：系统 Logo + 名称
- **中部**：主要功能按钮组
  - 加载模型
  - 开始分析
  - 剖切分析
  - 量测工具
  - 截图
  - 报告导出
- **右侧**：视图控制按钮（定位至模型、俯视/侧视/正视、全屏）
- 高度：48px
- 样式：深色背景（`#1a1a2e`），白色图标

#### 6.2.2 左侧图层面板

- 宽度：260px（可折叠）
- 内容：
  - 模型图层（显隐开关、透明度滑块）
  - 台面图层（显隐开关、标注密度调节）
  - 高亮图层（显隐开关、高亮样式选择）
  - 底图切换（影像图/地形图）
- 每个图层项含显隐复选框和设置展开箭头

#### 6.2.3 三维场景视口

- 占据中央主体区域
- Cesium Viewer 容器
- 右下角：坐标系信息、比例尺、指北针
- 左下角：分析进度提示（分析进行时显示）
- 右上角：导航控件（缩放滑块）

#### 6.2.4 右侧面板

- 宽度：320px（可折叠）
- 标签页切换：

**标签页 1 — 统计概览：**

```
┌─────────────────────────┐
│  合规性检查结果           │
├─────────────────────────┤
│  合规率                  │
│  ████████░░ 82%         │
│                         │
│  台面总数： 24           │
│  合规：    20           │
│  不合规：   4           │
│                         │
│  风险分布               │
│  🔴 高风险： 1          │
│  🟠 中风险： 2          │
│  🟡 低风险： 1          │
│  🟢 合规：  20          │
│                         │
│  [查看详细报告 →]       │
└─────────────────────────┘
```

**标签页 2 — 台面列表：**

```
┌─────────────────────────┐
│  台面清单               │
├─────────────────────────┤
│ 🔍 [搜索...]            │
├─────────────────────────┤
│ ⚠️ P-001  安全平台      │
│    高程: 1245m          │
│    宽度: 3.2m (最小)    │
│    风险: 高 🔴          │
│    [定位] [详情]        │
├─────────────────────────┤
│ ✅ P-002  清扫平台      │
│    高程: 1230m          │
│    宽度: 7.5m           │
│    风险: 无 🟢          │
│    [定位] [详情]        │
├─────────────────────────┤
│ ...                     │
└─────────────────────────┘
```

**标签页 3 — 阈值配置：**

```
┌─────────────────────────┐
│  合规阈值配置           │
├─────────────────────────┤
│  通用最小宽度           │
│  [=====●====] 5.0 m     │
│                         │
│  安全平台最小宽度       │
│  [=====●====] 5.0 m     │
│                         │
│  清扫平台最小宽度       │
│  [======●===] 6.0 m     │
│                         │
│  运输平台最小宽度       │
│  [========●=] 8.0 m     │
│                         │
│  高级设置               │
│  ├ 坡度阈值: 15°        │
│  ├ 最小台面面积: 10m²   │
│  └ 测量间距: 2m         │
│                         │
│  [应用配置] [恢复默认]  │
└─────────────────────────┘
```

#### 6.2.5 底部状态栏

- 高度：28px
- 左侧：鼠标位置坐标（经度、纬度、高程）
- 中部：当前操作提示
- 右侧：性能信息（FPS、已加载瓦片数）

### 6.3 交互细节

| 交互场景 | 行为描述 |
|----------|----------|
| 鼠标悬停台面 | 台面高亮，显示信息气泡（类型、高程、宽度范围） |
| 点击台面 | 选中台面，右侧面板跳转至该台面详情，相机聚焦 |
| 点击台面列表项 | 三维场景飞行定位至该台面 |
| 右键台面 | 弹出上下文菜单（修改类型、删除、手动测量） |
| 拖拽剖切面 | 实时更新剖切效果和断面标注 |
| 阈值滑块拖动 | 延迟 500ms 后自动重新检查并更新高亮（防抖） |
| 分析进行中 | 显示进度条，禁止重复触发分析 |

### 6.4 视觉设计规范

| 项目 | 规范 |
|------|------|
| 主色调 | `#1890ff`（科技蓝） |
| 背景色 | `#0d1117`（深色模式）/ `#ffffff`（亮色模式） |
| 面板背景 | `rgba(26, 26, 46, 0.9)` |
| 合规色 | `#52c41a`（绿色） |
| 低风险色 | `#faad14`（黄色） |
| 中风险色 | `#fa8c16`（橙色） |
| 高风险色 | `#f5222d`（红色） |
| 字体 | `Microsoft YaHei`, `PingFang SC`, `sans-serif` |
| 标注字号 | 14px（台面标签）/ 12px（宽度数值） |
| 圆角 | 4px（卡片）/ 2px（按钮） |

---

## 7. 数据需求

### 7.1 输入数据格式

#### 7.1.1 3D Tiles 模型数据

- 格式：3D Tiles 1.0/1.1 规范
- 入口文件：`tileset.json`
- 包含内容：
  - `asset.version`: 3D Tiles 版本
  - `geometricError`: 根瓦片几何误差
  - `root.boundingVolume`: 根瓦片边界范围（`region` 或 `sphere`）
  - `root.content.uri`: 内容瓦片文件路径（`.b3dm` / `.pnts` / `.i3dm` / `.cmpt`）
- 坐标参考系统：WGS84 经纬度/地心坐标
- 精度要求：模型平面精度 ≤ 0.5m，高程精度 ≤ 0.3m

#### 7.1.2 模型元数据（可选）

```json
{
  "mineName": "XX矿山",
  "mineType": "露天金属矿",
  "surveyDate": "2026-06-15",
  "surveyMethod": "倾斜摄影测量",
  "modelPrecision": 0.3,
  "coordinateSystem": "CGCS2000",
  "coverageArea": "2.5km²",
  "designParameters": {
    "benchHeight": 12,
    "slopeAngle": 65,
    "safetyPlatformWidth": 5,
    "cleaningPlatformWidth": 6,
    "transportPlatformWidth": 10
  }
}
```

### 7.2 输出数据格式

#### 7.2.1 分析结果 JSON

```json
{
  "analysisId": "uuid-xxx",
  "analysisTime": "2026-07-24T15:30:00+08:00",
  "mineName": "XX矿山",
  "modelSource": "tileset.json",
  "thresholds": {
    "safety_platform_min_width": 5.0,
    "cleaning_platform_min_width": 6.0,
    "transport_platform_min_width": 8.0,
    "slope_threshold": 15,
    "min_area": 10,
    "sample_interval": 2.0
  },
  "summary": {
    "totalPlatforms": 24,
    "compliantCount": 20,
    "nonCompliantCount": 4,
    "complianceRate": 0.833,
    "riskDistribution": {
      "high": 1,
      "medium": 2,
      "low": 1,
      "none": 20
    }
  },
  "platforms": [
    {
      "id": "P-001",
      "type": "safety",
      "elevation": 1245.3,
      "area": 85.6,
      "averageWidth": 4.8,
      "minWidth": 3.2,
      "maxWidth": 6.1,
      "isCompliant": false,
      "complianceRate": 0.6,
      "complianceIssues": ["局部宽度低于安全平台最小宽度5m"],
      "widthMeasurements": [
        {
          "position": [112.45, 34.67, 1245.3],
          "width": 3.2,
          "isCompliant": false,
          "riskLevel": "high"
        }
      ],
      "boundaryPolygon": [
        [112.4501, 34.6701, 1245.3],
        [112.4502, 34.6702, 1245.3]
      ]
    }
  ]
}
```

#### 7.2.2 报告格式

- PDF 报告：A4 纵向，含封面、正文、附图、附表
- Excel 报告：两个 Sheet（台面清单 + 统计汇总）

### 7.3 数据存储

| 数据类型 | 存储方式 | 说明 |
|----------|----------|------|
| 分析结果 | `localStorage` | 缓存最近 10 次分析结果 |
| 用户配置 | `localStorage` | 阈值、偏好等 |
| 三维模型 | 服务器静态文件 | 3D Tiles 数据集 |
| 导出报告 | 浏览器下载 | PDF / Excel 文件 |

---

## 8. 接口需求

### 8.1 内部模块接口

系统内部各模块通过事件总线（Event Bus）通信：

```javascript
// 事件总线 API
eventBus.on(eventName, callback);
eventBus.emit(eventName, data);
eventBus.off(eventName, callback);
```

| 事件名 | 触发源 | 数据 | 监听者 |
|--------|--------|------|--------|
| `model:loaded` | 模型加载模块 | tileset 对象 | 分析模块、图层模块 |
| `model:loading-progress` | 模型加载模块 | 进度百分比 | UI 状态栏 |
| `analysis:started` | 分析模块 | — | UI 进度条 |
| `analysis:progress` | 分析模块 | 当前步骤/总步骤 | UI 进度条 |
| `analysis:completed` | 分析模块 | Platform[] | 可视化模块、统计模块 |
| `analysis:error` | 分析模块 | 错误信息 | UI 提示 |
| `threshold:changed` | 阈值配置模块 | 新阈值 | 合规检查模块 |
| `platform:selected` | 三维交互模块 | Platform ID | 右侧面板 |
| `platform:updated` | 人工修正模块 | 修正后的 Platform | 可视化模块、统计模块 |

### 8.2 外部接口（可选后端）

如果系统扩展为前后端分离架构，需要以下 API：

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 上传模型 | POST | `/api/models/upload` | 上传 3D Tiles 数据 |
| 获取模型列表 | GET | `/api/models` | 获取已上传模型 |
| 保存分析结果 | POST | `/api/analysis/save` | 保存分析结果到服务端 |
| 获取历史结果 | GET | `/api/analysis/history` | 获取历史分析记录 |
| 生成报告 | POST | `/api/reports/generate` | 服务端生成报告 |

---

## 9. 非功能性需求

### 9.1 性能需求

| 指标 | 要求 |
|------|------|
| 模型加载时间 | ≤ 10s（100MB 模型，10Mbps 带宽） |
| 首屏渲染时间 | ≤ 5s |
| 分析计算时间 | ≤ 30s（覆盖 1km²、0.5m 采样精度） |
| 高亮刷新时间 | ≤ 2s（阈值变更后） |
| 帧率（浏览场景） | ≥ 30 FPS |
| 帧率（分析渲染） | ≥ 25 FPS |
| 内存占用 | ≤ 2GB（浏览器进程增量） |

### 9.2 可靠性需求

- 分析过程中出现异常时，已完成的步骤结果不丢失
- Web Worker 崩溃时自动降级为主线程计算（并提示用户性能可能下降）
- 模型加载失败时显示明确的错误信息和重试按钮
- 浏览器崩溃/刷新后，从 `localStorage` 恢复上次分析结果和配置

### 9.3 可用性需求

- 所有操作有明确的视觉反馈（加载动画、成功/失败提示）
- 关键操作（删除台面、重新分析）需二次确认
- 支持键盘快捷键（Ctrl+S 保存配置、Ctrl+Z 撤销等）
- 响应式设计适配不同屏幕分辨率（最低 1280×720）

### 9.4 安全性需求

- 不将矿山敏感数据上传至第三方服务
- 所有计算在本地浏览器完成（离线可用）
- 如有后端，使用 HTTPS 传输
- 模型数据访问需鉴权（如有后端）

### 9.5 可维护性需求

- 代码模块化，核心算法、可视化、UI 分层
- 关键算法函数有完整 JSDoc 注释
- 配置项集中管理，便于调整
- 提供开发文档和组件说明

### 9.6 兼容性需求

- 主流浏览器：Chrome ≥ 90、Edge ≥ 90、Firefox ≥ 88
- 操作系统：Windows 10+、macOS 10.15+、Ubuntu 20.04+
- 不支持移动端（因三维渲染性能和交互需求）

---

## 10. 技术架构与依赖

### 10.1 技术选型

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 三维引擎 | CesiumJS | ^1.115 | 核心三维渲染和分析 |
| 前端框架 | Vue 3 | ^3.4 | UI 组件化（可选 React，推荐 Vue） |
| 构建工具 | Vite | ^5.0 | 快速构建和热更新 |
| UI 组件库 | Element Plus | ^2.6 | 界面组件（Vue 配套） |
| 状态管理 | Pinia | ^2.1 | Vue 状态管理 |
| 几何计算 | turf.js | ^7.0 | 二维几何运算（边界、面积等） |
| 三角剖分 | d3-delaunay | ^6.0 | Delaunay 三角剖分（Alpha Shape 用） |
| PDF 导出 | jsPDF + html2canvas | latest | 报告生成 |
| Excel 导出 | SheetJS (xlsx) | ^0.18 | Excel 导出 |
| Web Worker | 原生 Worker API | — | 并行计算 |

### 10.2 架构分层

```
┌─────────────────────────────────────┐
│           表现层 (Presentation)       │
│  Vue Components + Element Plus       │
│  ├─ ToolBar.vue                      │
│  ├─ LayerPanel.vue                   │
│  ├─ SceneViewer.vue                  │
│  ├─ InfoPanel.vue                    │
│  └─ StatusBar.vue                    │
├─────────────────────────────────────┤
│           业务层 (Business)           │
│  ├─ ModelManager      (模型管理)      │
│  ├─ AnalysisEngine    (分析引擎)      │
│  ├─ ComplianceChecker (合规检查)      │
│  ├─ ReportGenerator   (报告生成)      │
│  └─ ToolManager       (场景工具)      │
├─────────────────────────────────────┤
│         算法层 (Algorithm)            │
│  ├─ DEMGenerator      (DEM 生成)      │
│  ├─ SlopeCalculator   (坡度计算)      │
│  ├─ PlatformDetector  (台面识别)      │
│  ├─ SkeletonExtractor (骨架提取)      │
│  ├─ WidthMeasurer     (宽度测量)      │
│  └─ BoundaryExtractor (边界提取)      │
├─────────────────────────────────────┤
│         渲染层 (Rendering)            │
│  ├─ CesiumRenderer    (Cesium 渲染)   │
│  ├─ AnnotationManager (标注管理)      │
│  ├─ HighlightManager  (高亮管理)      │
│  └─ AnimationManager  (动画管理)      │
├─────────────────────────────────────┤
│         数据层 (Data)                 │
│  ├─ TilesetLoader     (数据加载)      │
│  ├─ ResultCache       (结果缓存)      │
│  ├─ ConfigStore       (配置存储)      │
│  └─ ExportService     (导出服务)      │
└─────────────────────────────────────┘
```

### 10.3 核心依赖包清单（package.json）

```json
{
  "dependencies": {
    "cesium": "^1.115.0",
    "vue": "^3.4.0",
    "pinia": "^2.1.0",
    "element-plus": "^2.6.0",
    "@turf/turf": "^7.0.0",
    "d3-delaunay": "^6.0.4",
    "jspdf": "^2.5.1",
    "html2canvas": "^1.4.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "vite-plugin-static-copy": "^1.0.0"
  }
}
```

> **注意**：Cesium 需要额外的静态资源处理。Vite 配置中需使用 `vite-plugin-static-copy` 将 `cesium/Build/Cesium` 目录下的静态资源（Workers、Assets、Widgets、ThirdParty）拷贝到输出目录。

---

## 11. 项目目录结构

```
mine-slope-compliance/
├── public/
│   ├── cesium/                    # Cesium 静态资源
│   │   ├── Assets/
│   │   ├── Workers/
│   │   ├── ThirdParty/
│   │   └── Widgets/
│   └── data/                      # 示例数据
│       └── tileset.json
├── src/
│   ├── main.js                    # 应用入口
│   ├── App.vue                    # 根组件
│   ├── components/                # Vue 组件
│   │   ├── layout/
│   │   │   ├── ToolBar.vue        # 顶部工具栏
│   │   │   ├── LayerPanel.vue     # 左侧图层面板
│   │   │   ├── SceneViewer.vue    # 中央三维视口
│   │   │   ├── InfoPanel.vue      # 右侧信息面板
│   │   │   └── StatusBar.vue      # 底部状态栏
│   │   ├── scene/
│   │   │   ├── CesiumContainer.vue
│   │   │   ├── ProgressIndicator.vue
│   │   │   └── CoordinateDisplay.vue
│   │   ├── analysis/
│   │   │   ├── StatisticsOverview.vue  # 统计概览
│   │   │   ├── PlatformList.vue        # 台面列表
│   │   │   ├── PlatformDetail.vue      # 台面详情
│   │   │   └── ThresholdConfig.vue     # 阈值配置
│   │   ├── tools/
│   │   │   ├── MeasureTool.vue
│   │   │   ├── ClipTool.vue
│   │   │   └── BookmarkTool.vue
│   │   └── common/
│   │       ├── ConfirmDialog.vue
│   │       └── LoadingOverlay.vue
│   ├── modules/                   # 业务模块
│   │   ├── model/
│   │   │   ├── ModelManager.js
│   │   │   └── TilesetLoader.js
│   │   ├── analysis/
│   │   │   ├── AnalysisEngine.js
│   │   │   ├── DEMGenerator.js
│   │   │   ├── SlopeCalculator.js
│   │   │   ├── PlatformDetector.js
│   │   │   ├── SkeletonExtractor.js
│   │   │   ├── WidthMeasurer.js
│   │   │   └── BoundaryExtractor.js
│   │   ├── compliance/
│   │   │   └── ComplianceChecker.js
│   │   ├── rendering/
│   │   │   ├── CesiumRenderer.js
│   │   │   ├── AnnotationManager.js
│   │   │   ├── HighlightManager.js
│   │   │   └── AnimationManager.js
│   │   ├── report/
│   │   │   ├── ReportGenerator.js
│   │   │   ├── PDFExporter.js
│   │   │   └── ExcelExporter.js
│   │   └── tools/
│   │       ├── MeasureManager.js
│   │       ├── ClipManager.js
│   │       └── ScreenshotManager.js
│   ├── workers/                   # Web Worker
│   │   ├── demWorker.js           # DEM 生成 Worker
│   │   ├── slopeWorker.js         # 坡度计算 Worker
│   │   ├── platformWorker.js      # 台面识别 Worker
│   │   └── widthWorker.js         # 宽度测量 Worker
│   ├── stores/                    # Pinia 状态管理
│   │   ├── modelStore.js
│   │   ├── analysisStore.js
│   │   ├── configStore.js
│   │   └── uiStore.js
│   ├── utils/                     # 工具函数
│   │   ├── cesiumUtils.js         # Cesium 辅助函数
│   │   ├── geometryUtils.js       # 几何计算
│   │   ├── colorUtils.js          # 颜色管理
│   │   ├── formatUtils.js         # 格式化
│   │   └── eventBus.js            # 事件总线
│   ├── types/                     # 类型定义
│   │   └── index.d.ts
│   └── assets/                    # 静态资源
│       ├── icons/
│       └── styles/
│           ├── variables.scss
│           └── global.scss
├── vite.config.js
├── package.json
└── README.md
```

---

## 12. 验收标准

### 12.1 功能验收

| 编号 | 验收项 | 验收标准 | 优先级 |
|------|--------|----------|--------|
| AC-01 | 模型加载 | 能成功加载 3D Tiles 模型并流畅渲染 | P0 |
| AC-02 | 模型定位 | 「定位至模型」按钮可飞至模型范围 | P0 |
| AC-03 | DEM 生成 | 能从模型表面采样生成 DEM 网格 | P0 |
| AC-04 | 坡度计算 | 能正确计算每个采样点的坡度角 | P0 |
| AC-05 | 台面识别 | 能识别出近水平面的台面区域 | P0 |
| AC-06 | 边界提取 | 能提取台面边界多边形 | P0 |
| AC-07 | 骨架提取 | 能提取台面中心线 | P0 |
| AC-08 | 宽度测量 | 能沿中心线测量各点台面宽度 | P0 |
| AC-09 | 宽度标注 | 能在三维场景中显示宽度标注线和数值 | P0 |
| AC-10 | 阈值配置 | 用户可配置最小宽度阈值 | P0 |
| AC-11 | 不合规高亮 | 宽度 < 阈值的区域以红色高亮显示 | P0 |
| AC-12 | 统计概览 | 显示台面总数、合规率、风险分布 | P0 |
| AC-13 | 台面列表 | 可查看台面清单并点击定位 | P0 |
| AC-14 | 风险等级 | 不合规台面显示对应风险等级颜色 | P1 |
| AC-15 | 手动测量 | 提供手动两点测量工具 | P1 |
| AC-16 | 剖切分析 | 支持模型剖切并显示断面 | P1 |
| AC-17 | 报告导出 | 可导出 PDF 和 Excel 报告 | P1 |
| AC-18 | 人工修正 | 可删除误识别台面、手动绘制新台面 | P1 |
| AC-19 | 透明度控制 | 可调节模型透明度 | P1 |
| AC-20 | 截图功能 | 可截取场景画面 | P1 |

### 12.2 性能验收

| 编号 | 验收项 | 验收标准 |
|------|--------|----------|
| AC-P01 | 加载性能 | 100MB 模型加载 ≤ 10s |
| AC-P02 | 渲染性能 | 浏览帧率 ≥ 30 FPS |
| AC-P03 | 分析性能 | 1km² 区域分析 ≤ 30s |
| AC-P04 | 响应性能 | 阈值变更后高亮更新 ≤ 2s |

### 12.3 兼容性验收

| 编号 | 验收项 | 验收标准 |
|------|--------|----------|
| AC-C01 | Chrome | Chrome 90+ 正常运行 |
| AC-C02 | Edge | Edge 90+ 正常运行 |
| AC-C03 | Firefox | Firefox 88+ 正常运行 |

---

## 13. 附录

### 13.1 默认配置参数汇总

```yaml
# 模型加载
model:
  maximumScreenSpaceError: 16
  maximumMemoryUsage: 1024
  dynamicScreenSpaceError: true
  skipLevelOfDetail: true

# 分析参数
analysis:
  samplingResolution: 0.5        # 采样间距（米）
  maxSamplingPoints: 500000      # 最大采样点数
  slopeThreshold: 15             # 坡度阈值（度）
  minPlatformArea: 10            # 最小台面面积（平方米）
  widthSampleInterval: 2.0       # 宽度测量间距（米）

# 合规阈值
compliance:
  safetyPlatformMinWidth: 5.0    # 安全平台最小宽度（米）
  cleaningPlatformMinWidth: 6.0  # 清扫平台最小宽度（米）
  transportPlatformMinWidth: 8.0 # 运输平台最小宽度（米）
  generalMinWidth: 5.0           # 通用最小宽度（米）

# 风险等级
riskLevels:
  high:
    ratio: 0.5                   # 宽度 < 阈值 × 50%
    color: "#f5222d"
    label: "高风险"
  medium:
    ratio: 0.8
    color: "#fa8c16"
    label: "中风险"
  low:
    ratio: 1.0
    color: "#faad14"
    label: "低风险"
  none:
    color: "#52c41a"
    label: "合规"

# 可视化
visualization:
  compliantColor: "#00FF00"
  nonCompliantColor: "#FF0000"
  highlightFillAlpha: 0.4
  highlightPulse: true
  highlightPulsePeriod: 2000
  annotationFontSize: 12
  annotationDensity: "medium"    # sparse / medium / dense
```

### 13.2 关键 Cesium API 参考

| 功能 | Cesium API |
|------|------------|
| 加载 3D Tiles | `Cesium.Cesium3DTileset.fromUrl(url)` |
| 飞至模型 | `viewer.flyTo(tileset)` |
| 高程采样 | `viewer.scene.sampleHeight(cartographic)` |
| 精确高程采样 | `Cesium.sampleTerrainMostDetailed(terrainProvider, positions)` |
| 世界坐标↔屏幕坐标 | `scene.cartesianToCanvasCoordinates(cartesian3)` |
| 射线求交 | `scene.pick(ray)` / `globe.pick(ray, scene)` |
| 剖切平面 | `tileset.clippingPlanes = new Cesium.ClippingPlaneCollection({...})` |
| 实体标注 | `viewer.entities.add({ position, label: {...}, polyline: {...} })` |
| 多边形 | `viewer.entities.add({ polygon: { hierarchy: new Cesium.PolygonHierarchy(positions), ...} })` |
| 自定义材质 | `new Cesium.Material({ fabric: {...} })` |
| 屏幕坐标拾取 | `scene.pickPosition(windowPosition)` |
| 相机控制 | `camera.flyTo({ destination, orientation })` |

### 13.3 测试数据建议

为验证系统功能，建议准备以下测试数据：

1. **理想测试模型**：人工创建的含规则台阶的矿山模型，台面宽度已知（用于验证测量精度）
2. **真实矿山模型**：实际矿山的倾斜摄影测量数据
3. **边界测试模型**：
   - 极窄台面（宽度 1~3m）
   - 极宽台面（宽度 > 20m）
   - 不规则形状台面
   - 含植被遮挡的模型
4. **性能测试数据**：大规模模型（> 500MB），用于压力测试

### 13.4 后续迭代方向（不在本期范围）

1. 时序对比分析：对比不同时期的模型，检测台面宽度变化趋势
2. AI 深度学习识别：使用训练好的模型直接从点云中分割台面
3. 边坡稳定性分析：结合坡面角、台阶高度进行综合稳定性评估
4. VR/AR 支持：支持 VR 设备沉浸式检查
5. 多人协同检查：支持多人同时在线标注和审查
6. 无人机数据实时接入：对接无人机实时视频流进行现场检查

---

**文档结束**

> 本需求规格说明书作为后续 AI 编码执行的基线文档。AI 在执行过程中如发现需求歧义或技术可行性问题，应优先参考本文档的约束和方案，必要时向需求方确认后方可调整。
